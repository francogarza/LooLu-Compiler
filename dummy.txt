LooLu miPrograma;

CLASSES:
    Class Empleado {
        VARS:
            var int : edad;
            var char : nombre[30];
            var flotante: sueldo;

        FUNCS:
            func disminuirEdad ( x : int , b : int ) int {
                return ( edad - x );
            }
    }

VARS:
  var int : a;
  var flotante : x;
  var bool : b, c, d;

FUNCS:
  func incrementaSueldo(emp: Empleado, porcentaje: float) void {
  	emp.sueldo = emp.sueldo * porcentaje;
  }

Loo() {
  lectura(a,b,c);
  disminucionSueld = empleadoUno.disminuirEdad(10);
  print(disminucionSueld);

  incrementaSueldo(empleadoUno, 0.2);
  print(disminucionSueldo);

  if (a + 24) {
    a = b + 23;
  }



} Lu;