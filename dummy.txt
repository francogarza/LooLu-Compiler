LooLu miPrograma;

VARS:
  var int : a;
  var float : x;
  var bool : b, c, d;

FUNCS:
  func void incrementaSueldo(emp: Empleado, porcentaje: float) {
  	emp.sueldo = emp.sueldo * porcentaje;
  }
  func void incrementaSueldo2(emp: Empleado, porcentaje: float) {
  	emp.sueldo = emp.sueldo * porcentaje;
  }

CLASSES:
  Class Empleado {
    VARS:
      var int : a;
      var float : x, y;
    FUNCS:
      func void asdf3(emp: Empleado, porcentaje: float)  {
        emp.sueldo = emp.sueldo * porcentaje;
      }
  }
  Class Persona {
    VARS:
      var int : a;
      var float : x, y;
    FUNCS:
      func void asdf(emp: Empleado, porcentaje: float)  {
        emp.sueldo = emp.sueldo * porcentaje;
      }
      func void asdf3(emp: Empleado, porcentaje: float)  {
        emp.sueldo = emp.sueldo * porcentaje;
      }
  }

Loo() {
  lectura(a,b,c);
  disminucionSueldo = empleadoUno.disminuirEdad(10);
  print(disminucionSueldo);

  incrementaSueldo(empleadoUno, 0.2);
  print(disminucionSueldo);

  if (empleadoUno.disminuirEdad(10)) {
    a = b + 23;
  } else {
    print(Hi);
  }

  while (empleadoUno.disminuirEdad(10)) {
    while (empleado.edad > 5) {
      empleado.edad = empleado.edad - 1;
    }
  }



} Lu;
