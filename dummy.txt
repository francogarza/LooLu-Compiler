LooLu miPrograma;

classes:
    Class Empleado {
    vars:
        var int : edad;
        var char : nombre[30];
        var flotante: sueldo;

    funcs:
        func disminuirEdad ( x : int , b : int ) int {
            return ( edad - x );
        }
    }

vars:
  var int : a;
  var flotante : disminucionSueldo;
  var bool : b, c, d;

funcs:
  func incrementaSueldo(emp: Empleado, porcentaje: float) void {
  	emp.sueldo = emp.sueldo * porcentaje;
  }

Loo() {

} Lu;