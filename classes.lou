LooLu miPrograma;

VARS:
    var int : temp;
FUNCS:
CLASSES:
    class persona{
        VARS:
            var int : edad;
            var float : altura, altura2;
        FUNCS:
            func int returnAge(n : int, x: int){
                VARS: 
                START:
                    return(n);
            }
    }

Loo() {
    var franco = persona();
    var alvaro = persona(); 
} Lu;
