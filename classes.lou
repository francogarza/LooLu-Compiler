LooLu miPrograma;

VARS:
    var int : temp;
FUNCS:
CLASSES:
    class persona{
        VARS:
            var int : edad;
            var float : altura;
        FUNCS:
            func void returnAge(n : int){
                VARS: 
                START:
                    print(n);
            }
    }

Loo() {
    var franco = persona();
    franco.edad = 24;
    franco.altura = 1.78;
    print(franco.edad, franco.altura);
    franco.returnAge(4);
} Lu;
