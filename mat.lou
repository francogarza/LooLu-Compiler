LooLu miPrograma;

VARS:
  var int : myArr[10], myMat[10][10]; 
  var int : myInt, i, j, cont;

FUNCS:
  
CLASSES:

Loo() {

  i = 0;
  cont = 0;
  j = 0;

  while (i < 10) {
    j = 0;
    while (j < 10) {
      myMat[i][j] = cont;
      j = j + 1;
      cont = cont + 1;
    }
    i = i + 1;
  }

  i = 0;
  cont = 0;
  j = 0;

  while (i < 10) {
    j = 0;
    while (j < 10) {
      myInt = myMat[i][j];
      print(myInt);
      j = j + 1;
    }
    print();
    i = i + 1;
  }

} Lu;
