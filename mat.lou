LooLu miPrograma;

VARS:
  var int : myArr[10], myMat[3][3]; 
  var int : myInt, i, j, cont;

FUNCS:
  
CLASSES:

Loo() {

  i = 0;
  cont = 0;
  j = 0;

  while (i < 3) {
    j = 0;
    while (j < 3) {
      myMat[i][j] = cont;
      j = j + 1;
      cont = cont + 1;
    }
    i = i + 1;
  }

  i = 0;
  cont = 0;
  j = 0;

  while (i < 3) {
    j = 0;
    while (j < 3) {
      myInt = myMat[i][j];
      print(myInt);
      j = j + 1;
    }
    i = i + 1;
  }

} Lu;
