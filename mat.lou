LooLu miPrograma;

VARS:
  var int : myArr[10], myMat[10][10]; 
  var int : myInt, i, j, cont;

FUNCS:
<<<<<<< HEAD

func void fillMat(s1 : int, s2 : int) {
    VARS:
      var int : i, j, cont, act, myMat[10][10];
    START:
        i = 0;
        cont = 0;

      while (i < s1) {
        j = 0;
        while (j < s2) {
          myMat[i][j] = cont;
          cont = cont + 1;
          j = j + 1;
        }
        i = i + 1;
      }

      i = 0;

      while (i < s1) {
        j = 0;
        while (j < s2) {
          act = myMat[i][j];
          print(act);
          j = j + 1;
        }
        print();
        i = i + 1;
      }
     
}


func void printMat(s1 : int, s2 : int) {
    VARS:
      var int : i, j, act;
    START:
}


=======
    func void fillMat(s1 : int, s2 : int) {
        VARS:
        var int : i, j, cont, act, myMat[10][10];
        START:
            i = 0;
            cont = 0;
        while (i < s1) {
            j = 0;
            while (j < s2) {
            myMat[i][j] = cont;
            cont = cont + 1;
            j = j + 1;
            }
            i = i + 1;
        }
        i = 0;
        while (i < s1) {
            j = 0;
            while (j < s2) {
            act = myMat[i][j];
            print(act);
            j = j + 1;
            }
            print();
            i = i + 1;
        }     
    }
>>>>>>> 2c806af402b2ee52dc4909bd9cc17d2d3c792a87
CLASSES:

Loo() {

  fillMat(10, 10);

} Lu;
