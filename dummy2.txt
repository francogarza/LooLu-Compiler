LooLu miPrograma;

VARS:
  var int : myInt, myInt2;
  var bool : abc;
  var float : x;
  var char : myChar;
  var float : a, b, c, d, e, f, g, h, i, j, k, l, m;

FUNCS:
  func void incrementaSueldo(emp: int, porcentaje1: float) {
  }
  func void incrementaSueldo2(asdf: float, porcentaje2: float) {
  }

CLASSES:
  Class Empleado {
    VARS:
      var int : a;
      var float : x, y;
    FUNCS:
      func void asdf3(emp: float, porcentaje3: float)  {
      }
  }
  Class Persona {
    VARS:
      var int : a;
      var float : x, y;
    FUNCS:
      func void asdf(emp: int, porcentaje: float)  {
      }
      func void asdf3(emp: int, porcentaje: bool)  {
      }
  }

Loo() {

	abc = ((a * b - c * d) > c + d * e / f) && (g * h - j > l - a) && b + c > d * f;

  read(abc, a, b, c);
  print(abc, a, b, c);

  if (abc) {
    print(abc);
    read(a);
    a = b + c;
  }

} Lu;
