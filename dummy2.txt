LooLu miPrograma;

VARS:
  var int : myInt, myInt2;
  var float : myFloat;
  var bool : abc, cba;
  var float : x;
  var char : myChar;
  var float : a, b, c, d, e, f, g, h, i, j, k, l, m;

FUNCS:

CLASSES:

Loo() {
  a = 1.0;
  b = 2.0;
  c = 3.0;
  d = 6.0;
  e = c * 2;
  f = 45.5;
  i = b * f / d;
  j = 34.7;
  g = 3.14;
  h = 3.4;
  l = h * f;

  myInt = 3;

  abc = ((a * b - c * d) > c + d * e / f) && (g * h - j > l - a) && b + c > d * f;
  print(abc);
} Lu;
