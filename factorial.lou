LooLu miPrograma;

VARS:
  var int : myInt;

FUNCS:

func int factorial(n: int) {
    VARS:
    START:
     if (n < 2){
         return(1);
     } else {
         return(n * (factorial(n - 1)));
     }
}
CLASSES:

Loo() {

    myInt = -1;
    print(myInt);

    print(factorial(5));

} Lu;
