LooLu miPrograma;

VARS:
  var int : myArr[100], myInt, myArr2[2];
FUNCS: 
   func int fib(n : int) {
     VARS:
     START:
      if (n < 3){
        return(1);
      }
      return((fib(n-1)) + (fib(n-2)));
   }


CLASSES:

Loo() {
    myInt = myArr[6];
    print(myInt);

} Lu;
