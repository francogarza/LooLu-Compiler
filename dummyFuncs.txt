LooLu miPrograma;

VARS:
  var int : myArr[100], myInt, myArr2[2], myInt3, myInt4;
  var float : myFloat;
  var char : myChar;
  var bool : myBool;
FUNCS: 
   func char charfunc(n : char) {
     VARS:
     START:
      return(n);
   }

   func int fib(n : int) {
     VARS:
     START:
      if (n < 3){
        return(1);
      }
      return((fib(n-1)) + (fib(n-2)));
   }

   


CLASSES:

Loo() {
  myChar = charfunc('a');
  print(myChar);
} Lu;
